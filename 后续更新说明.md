# 卡丘大转盘：以后怎么更新

给以后改这个项目的人看（包括助手和陆泊霖自己）。

陆泊霖这台电脑上，成品在：

`C:\Users\87118\Desktop\卡拉彼丘大转盘`

平时双击 `卡丘大转盘.exe` 就能玩。同文件夹里的 `index.html` 用浏览器打开，适合改完先看效果。

---

## 先记住一件事

**名单在 `data.js` 里，但 exe 不会自动跟着变。**

exe 相当于把当时的网页封进一个程序里。你只改了旁边的 `data.js`，再双击旧程序，里面还是旧角色。

正确顺序：

1. 先改文件
2. 用浏览器打开 `index.html`，确认抽得对、头像能显示
3. 再重新做成 exe，覆盖原来的 `卡丘大转盘.exe`

| 你想改什么 | 改哪些文件 | 还要不要重做 exe |
| --- | --- | --- |
| 新角色、改阵营、改觉醒 | `data.js` + 头像图 | 要（如果继续用 exe） |
| 新副武器、近战、道具 | `data.js` | 要 |
| 改字、改颜色、改弹窗 | `index.html`、`style.css`、`app.js` | 要 |
| 改 exe 文件图标 | 先把图片转成 ico，再重做程序 | 要 |

---

## 一、加新角色

### 先查准名字

对照当期版本，不要编：

- https://wiki.biligame.com/klbq/角色筛选
- https://wiki.biligame.com/klbq/角色阵营
- https://strinova.org/wiki/Characters

在 `data.js` 的 `characters` 里加一条，例如：

```js
{ id:"xxxx", name:"中文名", side:"attacker", faction:"剪刀手",
  primary:"专属主武器名", awaken2:"觉醒2名字", awaken3:"觉醒3名字",
  portrait:"assets/portraits/xxxx.png" }
```

怎么填：

- `id`：英文小写，当文件名用，不要空格。头像文件名必须和它一样。
- `name`：界面只显示本名，不要写「某某·利里」这种后缀。
- `side` 决定进哪边的转盘：
  - `attacker`：只进攻（剪刀手）
  - `defender`：只防守（欧泊）
  - `both`：两边都能抽到（乌尔比诺）
- **晶源体不要加**，爆破模式选人没有他们。
- `primary` 是这个角色自带的主武器，不用单独转。
- 觉醒 2 和 3 每次只出其中一个，程序已经会处理。
- 同一轮进攻、防守不会抽到同一个人，也不用改程序。

### 头像放哪

放到项目里的：

`assets/portraits/xxxx.png`

（陆泊霖电脑上就是桌面「卡拉彼丘大转盘\assets\portraits\xxxx.png」）

用半身或正面、能剪成圆的图。没有头像时，转盘上只会显示名字的第一个字，会很难看，尽量补上。

---

## 二、加新枪、新近战、新道具

还是改 `data.js`：

- `secondaries`：副武器
- `melees`：近战
- `gadgets`：投掷道具（每边抽两个，且不重复）

一条长这样即可：

```js
{ id:"xxxx", name:"显示在圆圈里的名字", icon:"assets/icons/xxxx.svg" }
```

`icon` 现在用不到。界面定稿是圆圈里只写字，不用再补图标文件。

注意：道具里「破片」已经改叫 **手雷**，不要改回去。雪球这类活动道具，除非主人特别要求，否则不要加进去。

每个人被抽到的机会一样，不要偷偷加权。

---

## 三、改版本号

`data.js` 最上面有：

```js
version: "26SP4",
asOf: "2026-08-23",
```

游戏更新后改成当期即可。标题和弹窗上不要写版本号，主人不希望界面出现这些字。

---

## 四、重新做成能双击的程序

陆泊霖这台电脑上已经装好了 Python，路径是 `D:\python311\python.exe`。

打包请在英文路径下做，避免中文路径出错：

`C:\Users\87118\kpq-build`

做好后，再拷回桌面的「卡拉彼丘大转盘」文件夹。

步骤：

1. 把改过的 `index.html`、`app.js`、`data.js`、`style.css` 和整个 `assets` 文件夹，复制到 `C:\Users\87118\kpq-build`（新头像也要一起过去）。
2. 打开那里的 `launcher.py`，确认 **没有** `icon=` 这一项。现在用的网页窗口组件不认这个参数，写了程序会打不开。窗口最小大约 1100×760。
3. 程序图标不能直接用 jpg/png，要先转成 `icon.ico`（多种尺寸：16 到 256）。现在用的原图是 `icon1.jpg`。
4. 在 `kpq-build` 里执行：

```bat
D:\python311\python.exe -m PyInstaller --noconfirm --clean --onefile --windowed --name KaqiuSpinner --icon icon.ico --add-data "index.html;." --add-data "app.js;." --add-data "data.js;." --add-data "style.css;." --add-data "icon.jpg;." --add-data "assets;assets" launcher.py
```

5. 把生成的 `C:\Users\87118\kpq-build\dist\KaqiuSpinner.exe` 覆盖成桌面上的 `卡丘大转盘.exe`。如果提示文件正在使用，是旧程序还开着，先关掉再拷。不要长期留「卡丘大转盘-新.exe」这种重复文件。
6. 覆盖后，文件夹里图标可能还显示旧的，这是 Windows 记性不好。换个新文件名让主人看一眼，确认后再改回 `卡丘大转盘.exe`。
7. 桌面那个文件夹尽量只留能用的：exe、网页那几份、头像和两张底图、主人给的图标原图、这份说明。打包用的 `launcher.py`、`icon.ico` 不必堆在桌面。

补充：

- 打开时弹窗右边那张图，用的是 `icon.jpg`
- 文件图标用的是 `icon1.jpg` 转成的 ico
- 主人如果说「换图标」，先问清楚是弹窗里的图，还是 exe 那个小图标

---

## 五、界面已经定稿，不要随便改

除非主人当场改口：

- 标题就叫「卡丘大转盘」；进攻红、防守蓝
- 进攻：剪刀手 + 乌尔比诺；防守：欧泊 + 乌尔比诺
- 一屏里放下所有东西；电脑横屏左右两栏，竖起来就上下叠
- 整页白底，阵营图透在半透明卡片后面
- 副武器 / 近战 / 道具：圆圈里只写字，不要图标
- 觉醒格子写「觉醒 2 / 3」，圆圈里只写数字 2 或 3
- START 用比较方正的字
- 点头像换人：躲开刚抽过的人，觉醒跟着换，配装不动
- 打开时有说明弹窗，可以关掉。现在的三行（加粗、没有标点）是：
  - 点击START开转
  - 点击STOP停止喵
  - 点击角色头像可以换人喵
  - 右边偏右放 `icon.jpg`，靠左要渐变透明一些

转盘怎么抽，一般不用改 `app.js`。新角色只要 `side` 填对，会自动进对应那边。

---

## 六、拷到别人电脑

只需要拷 `卡丘大转盘.exe`。对方不用装 Python，也不用带网页文件。必须是 Windows。Win11 和近几年的 Win10 一般都有 Edge，就能开。第一次打开，系统可能提示「不明来源」，选仍要运行即可。特别老的系统如果打不开，再装微软的 WebView2 运行库。

---

## 七、谁干什么

- **幕僚长助手**：跟主人说话、拍板
- **工程助手**：改文件、重新打包
- **研究助手**：去查新版本角色和武器的官方名字，先讲结论，并写清出处；不要编

建议：研究助手先列出「新角色、阵营、觉醒 2/3、新枪新道具」，工程助手再改 `data.js` 并重做 exe。
