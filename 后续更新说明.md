# 卡丘大转盘 — 后续更新说明（给助手）

给以后接手的助手看。用户是陆泊霖，文件夹：

`C:\Users\87118\Desktop\卡拉彼丘大转盘`

能双击的成品是 `卡丘大转盘.exe`。`index.html` 同目录也能用浏览器打开做预览。

---

## 更新原理（先读这个）

数据和界面是两套东西：

| 改什么 | 改哪些文件 | 要不要重打 exe |
| --- | --- | --- |
| 新角色 / 新觉醒 / 改阵营 | `data.js` + 头像图 | **要**（exe 是打包快照，不会读旁边的 js） |
| 新副武器 / 近战 / 道具 | `data.js` | **要** |
| 只改字、样式、弹窗 | `index.html` / `style.css` / `app.js` | **要**（若用户用 exe） |
| 只改 exe 图标 | 源图 → `icon.ico` 再打包 | **要** |

只改桌面上的 `data.js`、双击旧 exe，**不会**出现新角色。exe 启动时读的是打进去的那份。

日常流程：先改桌面这份并打开 `index.html` 看对不对，确认后再重打 exe 覆盖 `卡丘大转盘.exe`。

---

## 1. 新角色

### 查资料（不要编名字）

以当期版本为准，交叉核对：

- https://wiki.biligame.com/klbq/角色筛选
- https://wiki.biligame.com/klbq/角色阵营
- https://strinova.org/wiki/Characters

写进 `data.js` 的 `characters` 数组，一条一个对象：

```js
{ id:"xxxx", name:"中文名", side:"attacker|defender|both", faction:"剪刀手|欧泊|乌尔比诺",
  primary:"专属主武器名", awaken2:"觉醒2名", awaken3:"觉醒3名",
  portrait:"assets/portraits/xxxx.png" }
```

规则：

- `id`：英文小写、文件名同名，不要空格。
- `name`：界面只显示本名，**不要**带「·利里」等后缀（玛德蕾娜、加拉蒂亚、奥黛丽、米雪儿都只留名）。
- `side`：
  - `attacker` = 只进进攻池（剪刀手）
  - `defender` = 只进防守池（欧泊）
  - `both` = 两边都能抽到（乌尔比诺）
- **晶源体不进爆破选人，不要加。**
- 主武器 `primary` 只跟角色走，**不单独转盘**。
- 觉醒 2 / 3 互斥，逻辑在 `app.js`，一般不用改。
- 同一抽进攻和防守不能是同一个人（`app.js` 已处理）。

### 头像

放到：

`C:\Users\87118\Desktop\卡拉彼丘大转盘\assets\portraits\<id>.png`

要求：正面或半身、能裁成圆；文件名 = `id`。缺图时界面会退回名字首字，但用户要好看，别缺。

`app.js` 会按 `portrait` 路径加载，不用改 JS，除非路径规则变了。

---

## 2. 新副武器 / 近战 / 道具

改 `data.js` 里对应数组：

- `secondaries` 副武器
- `melees` 近战
- `gadgets` 投掷道具（两边各抽 2 个且不重复）

每条：

```js
{ id:"xxxx", name:"显示名", icon:"assets/icons/xxxx.svg" }
```

`icon` 字段目前**没有用**。界面定稿是圆圈里只写字，不要往界面加图标。`assets/icons` 已删，不必为了新枪再补 svg。

命名注意：投掷里「破片」已改成 **手雷**，别改回去。季节/活动限定道具（如雪球）默认不进池，除非用户明确要。

随机是均匀 `Math.random()`，不要加权，除非用户说加。

---

## 3. 版本号

改 `data.js` 顶部：

```js
version: "26SP4",   // 改成当期，例如 26SP5
asOf: "2026-08-23",
```

弹窗和标题不要写版本号（用户不要界面上出现说明/版本字）。

---

## 4. 重打 exe（必须）

用户本机：

- Python：`D:\python311\python.exe`（已装 pywebview、PyInstaller、Pillow）
- 打包工作目录（拉丁路径，避免中文路径翻车）：`C:\Users\87118\kpq-build`
- 交付目录：桌面 `卡拉彼丘大转盘`

步骤：

1. 把桌面文件夹里改过的 `index.html` `app.js` `data.js` `style.css` 和 `assets\` **整份同步**到 `C:\Users\87118\kpq-build`（头像也要拷进 `kpq-build\assets\portraits`）。
2. 确认 `kpq-build\launcher.py` **没有** `icon=` 参数（当前 pywebview 的 `create_window` 不支持，会直接报错打不开）。窗口最小尺寸 `min_size=(1100, 760)`，`webview.start(gui="edgechromium")`。
3. exe 图标用 ico，不要把 jpg/png 直接丢给 PyInstaller：

```text
用 Pillow 把用户指定的图（现为桌面 icon1.jpg）裁成正方形，
导出 16/24/32/48/64/128/256 的 icon.ico 到 kpq-build。
```

4. 打包（在 `kpq-build` 下）：

```bat
D:\python311\python.exe -m PyInstaller --noconfirm --clean --onefile --windowed --name KaqiuSpinner --icon icon.ico --add-data "index.html;." --add-data "app.js;." --add-data "data.js;." --add-data "style.css;." --add-data "icon.jpg;." --add-data "assets;assets" launcher.py
```

说明弹窗右侧图用的是 `icon.jpg`（不是 exe 图标）。exe 图标是 `icon1.jpg` → `icon.ico`。用户若换图标，问清楚是窗口装饰图还是文件图标。

5. 覆盖：

`C:\Users\87118\kpq-build\dist\KaqiuSpinner.exe`
→
`C:\Users\87118\Desktop\卡拉彼丘大转盘\卡丘大转盘.exe`

若报 Permission denied：用户还开着旧窗口，先让他关掉再拷。不要长期留「卡丘大转盘-新.exe」之类重复文件，拷完删掉。

6. Windows 资源管理器会缓存 exe 图标。覆盖后 F5 仍可能是旧图。换一个新文件名让用户确认后，再覆盖回 `卡丘大转盘.exe`。图标已经打进 PE 资源里，不是旁边再放一张 ico。

7. 文件夹里只保留能跑的东西：`卡丘大转盘.exe`、`index.html`、`app.js`、`data.js`、`style.css`、`icon.jpg`、`icon.png`/`icon1.jpg`（用户的源图）、`assets\`（`portraits` + `bg\scissors-pic.png` + `bg\pus-pic.png`）、本说明。不要把 `launcher.py`、`icon.ico`、README、旧布局 html、说明书散落回去。

---

## 5. 界面定稿（不要擅自改）

除非用户当场改口：

- 标题「卡丘大转盘」；进攻红、防守蓝。
- 进攻池 = 剪刀手 + 乌尔比诺；防守池 = 欧泊 + 乌尔比诺。
- 一屏紧凑：横向两栏、竖向叠栏；整页白底；阵营底图在半透明卡片后面。
- 副武器 / 近战 / 道具：圆圈只写字，不要图标。
- 觉醒：标签「觉醒 2 / 3」，圈里只写 2 或 3。
- START 用方正一些的字（Bahnschrift）；点 START 开转，STOP 一次锁定全部。
- 点进攻/防守头像换人（避开刚抽过的，觉醒跟着换，配装不动）。
- 启动有可关闭说明弹窗，文案以用户最后一版为准（现为三行加粗、无标点）：
  - 点击START开转
  - 点击STOP停止喵
  - 点击角色头像可以换人喵
  - 右侧偏右铺 `icon.jpg`，左边渐变要透。

`app.js` 的抽取逻辑一般不用动。新角色只要 `side` 填对，会自动进对应池。

---

## 6. 换电脑分发

只拷 `卡丘大转盘.exe` 即可，不需要 Python、不需要旁边的 html。目标机是 Windows，一般要有 Edge / WebView2（Win11 和近几年 Win10 自带）。第一次可能被 SmartScreen 拦，选仍要运行。

---

## 7. 分工（本用户的助手）

- **幕僚长助手**：拍板、对用户说话、收口。
- **工程助手**：改代码、打包。
- **研究助手**：对版本、查新角色/武器官方名，结论先讲并标明来源。不要编。

更新名单时：研究助手先出「当期角色 + 阵营 + 觉醒2/3 + 新枪/新道具」对照表，工程助手再改进 `data.js` 并重打 exe。
